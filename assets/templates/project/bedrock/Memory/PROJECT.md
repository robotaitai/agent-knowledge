---
note_type: project-overview
project: <project-name>
updated: <date>
---

# Project

## What this project is

Short description.

## Current product direction

What are we building?

## How to navigate this memory

The Memory folder is project-shaped. Branches should match the real system,
product, or domain areas.

## Areas

One doc per area, `Memory/<area>.md`, holding everything durable about that
subsystem. Name areas after functional layers (navigation, perception, auth,
billing), not after hardware or folders, so cross-cutting subsystems get a home
instead of being split across this overview and `Memory/decisions.md`. Link each
one here as it is created; if work touches an area with no doc yet, create it.

- _(none yet)_

## Important context

Key things a new agent or developer should know.
